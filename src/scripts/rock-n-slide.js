var rockNslide = (function(config){
  // CONFIG
  const DEFAULT_CONFIG = {
    speed: 400,
    lazyLoad: true, // Req waypoints
    classList: {
      menuTrigger:       "menu-nav__trigger",
      menuTriggerActive: "menu-nav__trigger--active",
      menuSlide:         "slide--menu",
      menuSlideActive:   "slide--menu-active",
      menuItem:          "menu__nav-anchor",
      scrollText:        "slide__scrolling-text"
    }
  }
  // Check dependencies

  // Variables

  // Helpers

  // prevSlide

  // nextSlide

  // showMenu

  // Watchers

  return {
    scrollToTop: "scrollToTop",
    prevSlide: "prevSlide",
    nextSlide: "nextSlide",
    toggleMenu: "toggleMenu"
  }
})();
